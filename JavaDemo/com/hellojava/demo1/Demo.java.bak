package com.hellojava.demo1;
public class Demo 
{
	public static void main(String[] args) 
	{
		//在Student类的本身中没有定义任何属性和方法
		//name和eat() 是继承而来
		//new Student() 申请内存空间(Student 内部的属性和方法)
		//结论:创建子类的时候会先去创建父类
		//默认一定调用父类无参数的构造方法
		//可以在子类的构造方法中的第一句使用super(参数..) 指定调用父类的哪一个构造方法
		//如果重在了构造方法那么一般情况下都会重载一个无参数构造方法
		//Student s=new Student("张三");
		//s.name="张三";
		//s.eat();

		//Student.school="北京大学";
	//	Teacher.school="清华大学";

		//Student s=new Student();
		//s.name="张无忌";

		//Teacher t=new Teacher();
		//t.name="张三丰";
		//System.out.println(s.name);
		//System.out.println(t.name);


		//User user=new User(10,"张三","男",100);
		//user.show();
		
		/*
		User user=new User();

		user.setUserId(1);
		user.setUserName("张三");
		user.setUserSex("男");
		user.setUserAge(20);

		user.show();
		
		System.out.println(user.getUserName());
		*/

		Student s=new Student();
		s.showPersonId();
	}
}
